spring:
  # 数据源
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.114.141:3306/gulimall_oms
    driver-class-name: com.mysql.cj.jdbc.Driver

# MyBatis-Plus配置
mybatis-plus:
  # mapper.xml扫描
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      # 主键自增
      id-type: auto

server:
  port: 9000
